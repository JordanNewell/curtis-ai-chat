# Twitter / X — announce kit

**Use:** Post from your main account. Single tweet + 6-tweet thread. Best window: Tue–Thu 8–10am ET. Pin the thread for 48 hours after posting.

---

## Single tweet (≤280 chars)

```
Shipped Curtis AI Chat v1.0.0 — 30+ AI providers, agent tools that read/write your Obsidian vault, multi-model arena, voice I/O, inline diff rewrite, and 5 more features. MIT, no telemetry, no SaaS. Local-first via Ollama.

github.com/JordanNewell/curtis-ai-chat
```

Character count: ~270. Trim if needed.

## 6-tweet thread

### Tweet 1 (hook)

```
Just shipped Curtis AI Chat v1.0.0 — a polyglot AI chat plugin for Obsidian.

30+ providers (Anthropic, OpenAI, Gemini, Ollama, OpenRouter, 25+ more).
Agent tools. Multi-model arena. Voice. Diff rewrite. Memory.

MIT, no telemetry, local-first.

🧵 1/6
```

### Tweet 2 (the agent)

```
The flagship: Curtis Agent.

The AI calls nine tools to read, create, and edit notes in your vault — read_note, search_notes, create_note, edit_note, list_notes, get_tags, get_backlinks, get_current_note, calculator.

Opt-in. OpenAI-compat providers now, Anthropic/Gemini/Ollama in v4.1.

2/6
```

### Tweet 3 (arena + diff)

```
Multi-model arena: pick 2–5 models, send one prompt, watch responses stream side-by-side. Promote any column to main chat.

Inline diff rewrite: select text, Ctrl+Shift+R, AI generates an improved version, you get a green/red diff modal. Accept or reject. Cursor-style.

3/6
```

### Tweet 4 (voice + mentions + memory)

```

Voice I/O: Whisper STT on the mic, browser TTS on every reply.

@-mention: type @, fuzzy-search your vault, attach note content as context.

Memory: durable facts in a markdown file you can read + edit, now editable from Settings.

4/6
```

### Tweet 5 (no lock-in + local-first)

```
No vendor lock-in. Switch models mid-conversation. Add any OpenAI-compatible endpoint as a custom provider in 30 seconds.

Local-first: enable Ollama and nothing ever leaves your machine. Same plugin, same vault, same history.

Your data stays yours.

5/6
```

### Tweet 6 (install + CTA)

```
Install:
- Community plugin directory (v4 review pending)
- BRAT beta channel: JordanNewell/curtis-ai-chat
- Manual: download main.js + manifest.json + styles.css from latest release

Repo: github.com/JordanNewell/curtis-ai-chat

Bug reports, feature requests → GitHub Issues.

6/6
```

## Companion tweets (post 30–60 min after main thread)

### Reply to tweet 1 — screenshot album

Drop 4 screenshots:
1. Chat sidebar in action with the new header pills
2. Arena view with 3 models streaming
3. Diff rewrite modal (green/red)
4. Settings → Agent panel

### Standalone — comparison tweet

```
How Curtis AI Chat compares:

                  Curtis   Smart Conn   Text Gen   Copilot
Agent tools         9         —            —        partial
Providers          30+       1–2          1–2       5–10
Arena               ✓         —            —          —
Diff rewrite        ✓         —            —          —
Voice               ✓         —            —          —
Local-first         ✓         —            ✓          ✓

Different tools for different jobs. Source: github.com/JordanNewell/curtis-ai-chat
```

## Mastodon cross-post

Same as Tweet 1 + a markdown-formatted version of the thread on your Mastodon account. Use the extra character budget to add `#Obsidian #OpenSource #AI` hashtags at the end.
